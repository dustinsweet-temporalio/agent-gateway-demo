# Version floors known to carry the APIs this demo uses (update-with-start in
# temporalio and streamable HTTP in mcp). Install versions within the declared
# ranges below.
temporalio>=1.30.0
mcp>=1.12.0
google-adk[mcp]>=2.5.0,<3.0.0
requests>=2.31.0
starlette>=0.37.0
uvicorn>=0.30.0
python-multipart>=0.0.9
